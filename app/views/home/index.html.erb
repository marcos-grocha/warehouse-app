<br>
<section id="warehouses">
    <h2>Galpões</h2>
    <div>
        <%= link_to('Cadastrar Galpão', new_warehouse_path) %>
    </div>
    
    <div class="warehouse-list">
        <% @warehouses.each do |w| %>
            <div class="warehouse">
                <h3><%= link_to w.name, warehouse_path(w.id), class: 'btn btn-secondary' %></h3>
                <strong>Código</strong>: <%= w.code %><br>
                <strong>Cidade</strong>: <%= w.city %><br>
                <strong>Área</strong>: <%= w.area %> m2
            </div>
        <% end %>
    </div>
    
    <% if @warehouses.empty? %>
        <p>Não existem galpões cadastrados!</p>
    <% end %>
</section>